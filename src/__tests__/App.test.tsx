import { describe, expect, it } from 'vitest'
import { render, screen } from '@testing-library/react'

describe('App', () => {
    test("basket counter is updated on add to basket", () => {

        // render(<App />);
    })
})
